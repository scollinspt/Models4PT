Patient-specific instantiation of the refined model, and the reasoning trace
produced when applying it to a case — the proto-CIE step. Since the Clinical
Inference Engine doesn't exist as software yet, this is expected to start out
manual/narrative (or a lightweight notebook), used to discover what CIE
actually needs to do.

Nothing here yet.
